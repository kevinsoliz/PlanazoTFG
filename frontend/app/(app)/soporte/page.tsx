import ContactoView from "@/app/components/views/ContactoView";

// Reutiliza ContactoView, igual que /contacto en la landing. Mismo contenido, distinto layout.
export default function SoportePage() {
  return <ContactoView />;
}
